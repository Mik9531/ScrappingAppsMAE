version: "3.8"
services:
  flask:
    build:

      context: .
      dockerfile: Dockerfile

    env_file:
      - .env
    ports:
      - ${PORT}:8080  # I want to set port defined in the env file
    volumes:
      - .:/server
    restart: always